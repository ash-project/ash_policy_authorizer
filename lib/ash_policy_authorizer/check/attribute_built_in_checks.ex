defmodule AshPolicyAuthorizer.Check.AttributeBuiltInChecks do
  # def setting(opts) do
  #   {AshPolicyAuthorizer.Check.SettingAttribute, Keyword.take(opts, [:to])}
  # end

  # def logged_in(), do: {AshPolicyAuthorizer.Check.LoggedIn, []}
end
